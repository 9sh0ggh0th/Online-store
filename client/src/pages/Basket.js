import React, { useEffect } from "react";

const Basket = () => {

    // useEffect(() => {
    //     if(devicePixelRatio.id === active){

    //     }
    // }, [])

    return(
		<div>

        </div>
    );
};

export default Basket;